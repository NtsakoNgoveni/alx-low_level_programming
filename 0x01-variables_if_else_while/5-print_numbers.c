#include <stdio.h>
/**
 * main - prints single digit numbers of base starting 0
 * Return: 0 (success)
 */
int main(void)
{
	int n = 0;

	for (n = 0; n < 10 ; n++)
	{	printf("%d", n);
	}
	return (0);
}
